apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-gpu-tenants
  namespace: {{ kube_prometheus_stack_namespace }}
  labels:
    grafana_dashboard: "1"   # sidecar.dashboards.enabled picks this up
data:
  gpu-tenants-dashboard.json: |
{{ lookup('file', role_path ~ '/files/gpu-tenants-dashboard.json') | indent(4, true) }}
