apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-gpu-llm-billing
  namespace: {{ kube_prometheus_stack_namespace }}
  labels:
    grafana_dashboard: "1"
data:
  gpu-llm-billing-dashboard.json: |
{{ lookup('file', role_path ~ '/files/gpu-llm-billing-dashboard.json') | indent(4, true) }}
