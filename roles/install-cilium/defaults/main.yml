cilium_version: "1.19.2"
cilium_namespace: kube-system
# API server address used by Cilium's kube-proxy replacement
# Must match the K3s server node IP
k3s_api_server_host: "192.168.178.133"
k3s_api_server_port: "6443"
# Gateway API support (requires Gateway API CRDs to be installed first)
cilium_gateway_api_enabled: true
# Classic Ingress (networking.k8s.io/v1) support via Cilium
cilium_ingress_controller_enabled: true
