kagent_namespace: kagent
kagent_release_name: kagent
kagent_chart: "oci://ghcr.io/kagent-dev/kagent/helm/kagent"
kagent_chart_version: "0.9.11"   # última estable 0.9.x (0.10 aún beta) — 2026-07-06
# 2026-07-08: forzar registry ghcr.io. El default del chart es `registry: cr.kagent.dev`
# (el registry vanity de kagent) y su tag controller/ui:0.9.11 NO resuelve en nodos
# frescos → ImagePullBackOff eterno del controller (roto 40h). ghcr.io publica las
# mismas imágenes multi-arch (incl. arm64) y sí puldea. Ver commit de este fix.
kagent_image_registry: ghcr.io
kagent_hostname: kagent.cluster.home
kagent_node: srv-rk1-nvme-01

# 2026-06-10: kagent-tools pineado a un RK1 estable. Sin esto el scheduler lo
# mandaba a nodos Pi cuyo IP flapea por DHCP → MCP inaccesible para Hermes/OpenClaw.
kagent_tools_node: srv-rk1-nvme-03

# LiteLLM proxy as OpenAI-compatible backend (already deployed in ai namespace)
kagent_llm_model: "kagent-orchestrator"
kagent_llm_base_url: "http://litellm-proxy.ai.svc.cluster.local:4000/v1"
kagent_llm_api_key: "{{ litellm_master_key }}"

# Storage for bundled PostgreSQL
kagent_db_storage_class: longhorn-nvme
kagent_db_storage_size: 5Gi

# Namespaces to create with tenant RBAC (kagent writer + getter RoleBindings)
kagent_tenant_namespaces:
  - tenant-demo

# Built-in agents (k8s, helm, prometheus, cilium, istio, kgateway, argo)
kagent_agents_enabled: true
