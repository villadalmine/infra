# Full workstation requirements — installs core + ai + ops
# Used by: make deps-full  (or: pip install -r requirements.txt)
#
# For selective installs:
#   make deps          → requirements-core.txt only (mandatory)
#   make deps-ai       → requirements-core.txt + requirements-ai.txt
#   make deps-ops      → requirements-core.txt + requirements-ops.txt
#   make deps-full     → all three

-r requirements-core.txt
-r requirements-ai.txt
-r requirements-ops.txt
