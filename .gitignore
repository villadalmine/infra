# Secrets - never commit
*.vault
.env
.env.local
roles/*/defaults/secrets.yml
roles/*/defaults/secrets.yaml
roles/*/defaults/vault.yml
roles/*/defaults/.leloir_db_password
secrets/
vault/

# Survey output — per-run hardware snapshots, not versioned
/survey/

# AI tool local config overrides (user-specific)
opencode.local.json

# Python virtualenv (created by mise)
.venv/
__pycache__/
*.pyc

# mise local overrides (user-specific tool versions)
.mise.local.toml

# OpenClaw secrets
roles/install-openclaw/defaults/secrets.yml

# Credenciales temporales para sesiones de Claude (borrar después de usar)
.kubeconfig-claude
.kubeconfig
.sshkey-claude
